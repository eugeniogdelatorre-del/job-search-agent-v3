# Copy to .env for local scraper runs. Never commit .env.
# Production values live in GitHub Secrets (set via repo → Settings → Secrets and variables → Actions).

SUPABASE_URL=https://nqevtnhryjnlbzmiojyb.supabase.co
SUPABASE_SERVICE_KEY=

# Only needed once we reach Phases 3 / 6 / 8. Leave blank for Phase 1 scrape-only runs.
ANTHROPIC_API_KEY=
RESEND_API_KEY=
